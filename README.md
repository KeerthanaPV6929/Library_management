# Library_management
A Library Management System built with Python, Flask, HTML, and CSS. Features admin and member roles, book catalog with search, issue/return workflow, automatic fine calculation for late returns, and personal dashboards — all using in-memory data storage, no database required.
